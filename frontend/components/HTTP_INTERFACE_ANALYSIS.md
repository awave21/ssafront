# Анализ интерфейса работы с HTTP функциями

## Текущее состояние

Интерфейс имеет 3-колоночную структуру:
1. **Список функций** (слева) — список HTTP tools
2. **Редактор** (центр) — настройка endpoint, параметров, табы Auth/Body/Headers/Settings/Response
3. **Правая панель** — Input Schema + Test Console

## Проблемы и рекомендации

### ❌ ЧТО УБРАТЬ

#### 1. **Таб "Response" — избыточен для первого релиза**
**Проблема:**
- Трансформация ответов через JMESPath/выбор полей — это очень продвинутая функция
- Требует понимания структуры API, тестирования, итеративной настройки
- Backend пока не поддерживает `response_transform`
- 90% пользователей будут использовать сырой ответ

**Решение:**
- Убрать таб "Response" полностью на первом этапе
- Вернуть позже как advanced-фичу, когда базовый функционал будет отлажен
- В Test Console всегда показывать полный raw response

#### 2. **Таб "Settings" — непонятно, что там**
**Проблема:**
- В макете не показан контент
- Не ясно, какие настройки туда должны попасть
- Timeout/retry/domains — это скорее на уровне binding, а не tool

**Решение:**
- Убрать таб "Settings" для упрощения
- Если нужны настройки — вынести в отдельную секцию вне табов

#### 3. **Генерируемая Input Schema в правой панели — сбивает с толку**
**Проблема:**
- JSON Schema генерируется автоматически из Body parameters
- Показывается только для чтения (read-only)
- Занимает много места, но не редактируется
- Backend использует её для валидации, но пользователю это не нужно видеть постоянно

**Решение:**
- Убрать большую панель Schema из правой колонки
- Оставить только кнопку "View Schema" (опционально, модалка/коллапс)
- Освободить место для Test Console

---

### ✅ ЧТО ДОРАБОТАТЬ

#### 1. **Headers — обязательная фича**
**Проблема:**
- Сейчас таб "Headers" не реализован
- Многие API требуют кастомные заголовки (Content-Type, X-Api-Version, etc.)

**Решение:**
- Добавить таблицу key-value для headers (как Body Fields)
- Формат: `Header Name | Value | [x удалить]`
- Пример: `Authorization: Bearer {api_key}`, `Content-Type: application/json`

#### 2. **Auth — базовые типы**
**Проблема:**
- Таб "Auth" сейчас пустой
- Backend поддерживает `auth_type: "none" | "api_key" | "oauth2" | "service"`
- Нет UI для выбора credential_id

**Решение:**
- **Шаг 1 (MVP):** Выбор типа авторизации (dropdown):
  - None
  - API Key (+ выбор credential из списка)
  - OAuth2 (+ выбор credential)
  - Service Auth
- **Шаг 2:** Если выбран API Key/OAuth — показать dropdown с доступными credentials
- Сами credentials управляются на отдельной странице (не здесь)

#### 3. **Parameter Mapping — path/query/body**
**Проблема:**
- Сейчас все параметры идут в body
- Нет способа указать, что `user_id` из URL path: `/users/{user_id}`
- Нет query parameters: `/search?q={query}&limit=10`

**Решение:**
- Добавить колонку "Location" в таблице Body:
  - `Key | Location | Type | Value | [x]`
  - Location: dropdown `Body | Path | Query`
- Auto-detect path параметров из endpoint (парсинг `{param}`)
- Сохранять в `parameter_mapping: { user_id: 'path', query: 'query', name: 'body' }`

#### 4. **Test Console — улучшить UX**
**Текущие проблемы:**
- Консоль слишком маленькая (40% от правой панели)
- Payload textarea не подсказывает структуру
- Нет истории запросов
- Нет кнопки "Copy Response"

**Решение:**
- **Освободить место:** убрать Schema panel сверху → консоль займёт всю правую колонку
- **Payload preview:** подсказки из input_schema (autocomplete/validation)
- **Response:** 
  - Кнопка "Copy" для копирования ответа
  - Подсветка синтаксиса JSON
  - Вкладки: Raw / Formatted
- **Ошибки:** если 4xx/5xx — показать error body крупно

#### 5. **Body Fields → упростить типы**
**Проблема:**
- Текущие типы: String, Integer, Boolean, Array, Object
- Не хватает: Number (float), Null
- Array/Object требуют вложенного редактора (сложно)

**Решение (MVP):**
- Упростить до: `string | number | integer | boolean`
- Для array/object — использовать JSON mode (переключатель Fields/JSON)
- В JSON mode можно писать любую структуру

#### 6. **Индикация необходимости сохранения**
**Текущее:**
- Есть `●` в списке функций
- Есть галочка-кнопка сохранения

**Доработать:**
- Добавить tooltip на кнопку: "Сохранить изменения (Ctrl+S)"
- Keyboard shortcut: Ctrl+S / Cmd+S для быстрого сохранения
- После сохранения — toast "Saved" (уже есть)

---

### 🆕 ЧТО ДОБАВИТЬ

#### 1. **Quick Actions в списке функций**
**Идея:**
- Клик правой кнопкой → контекстное меню:
  - Duplicate function
  - Toggle Active/Inactive
  - Delete
  - Test (быстрый запуск без переключения)

#### 2. **Import/Export функций**
**Идея:**
- Кнопка "Import from cURL" — парсинг curl команды → заполнение полей
- Кнопка "Export as cURL" — генерация curl для тестирования вне UI
- Удобно для миграции с Postman/Insomnia

#### 3. **Validation errors в реальном времени**
**Идея:**
- Endpoint: проверка корректности URL
- Body parameters: подсветка дубликатов ключей
- Required fields: красная обводка если пусто

#### 4. **Environment variables**
**Идея:**
- Часто встречается: `https://{{base_url}}/api/v1/users`
- Добавить секцию Environment:
  - `base_url = api.example.com`
  - `api_version = v2`
- При тесте подставлять значения

---

## Приоритеты доработки

### 🔴 Критично (без этого не работает)
1. ✅ Убрать таб Response (пока не поддержан backend)
2. ✅ Убрать Schema panel из правой колонки → освободить место для консоли
3. ✅ Реализовать таб Headers (таблица key-value)
4. ✅ Реализовать таб Auth (dropdown типов + credential_id)
5. ✅ Добавить Parameter Mapping (Location: path/query/body)

### 🟡 Важно (улучшит UX)
6. ⚪ Улучшить Test Console (copy, history, better errors)
7. ⚪ Keyboard shortcuts (Ctrl+S для сохранения)
8. ⚪ Validation в реальном времени
9. ⚪ Auto-detect path parameters из URL

### 🟢 Позже (nice to have)
10. ⚪ Import/Export cURL
11. ⚪ Environment variables
12. ⚪ Context menu для функций
13. ⚪ Response transformation (вернуть позже)

---

## Итоговая структура (после доработки)

### Левая колонка (список)
- ✅ Список функций с фильтром
- ✅ Кнопка "+" для создания
- ✅ Индикаторы: HTTP method, active status, unsaved changes

### Центральная колонка (редактор)
- ✅ Название + описание (inline edit)
- ✅ Кнопка сохранения (галочка)
- ✅ Endpoint Configuration:
  - HTTP Method (dropdown)
  - URL (input с path parameters)
- ✅ Табы:
  - **Auth** (тип авторизации + credential)
  - **Body** (таблица: key, location, type, value)
    - Location: body/path/query
    - Переключатель Fields/JSON
  - **Headers** (таблица: key, value)
  - ~~Settings~~ (убрать)
  - ~~Response~~ (убрать на первом этапе)

### Правая колонка (test)
- ❌ ~~Input Schema (collapsed/hidden)~~ → убрать
- ✅ Test Console (на всю высоту):
  - Payload input (textarea с подсказками)
  - Run button
  - Response output (с copy, highlight, error handling)

---

## Вывод

**Упростить до MVP:**
- Убрать: Response tab, Settings tab, большую Schema panel
- Доработать: Auth, Headers, Parameter Mapping (path/query/body)
- Улучшить: Test Console (занимает всю правую колонку)

**Фокус:** простая и быстрая настройка HTTP endpoint → тест → работает.
