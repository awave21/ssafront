#!/usr/bin/env bash
# Placeholder for datamodel-codegen / OpenAPI sync from backend Pydantic (wave 6.1).
# Replace with your project's generator once `FlowDefinition` is exported via OpenAPI.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
echo "TODO: generate TS types from backend — see backend/app/schemas/script_flow_definition.py"
echo "Workspace root: $ROOT"
